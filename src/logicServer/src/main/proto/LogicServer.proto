syntax = "proto3";

option java_multiple_files = true;
option java_package = "via.sep3.logicserver.protobuf";

// Definition

service LogicServer {
    rpc createMember(CreatedMember) returns(ResponseMember);
}

message CreatedMember {
    string username = 1;
    string password = 2;
}

message ResponseMember {
    string username = 2;
}